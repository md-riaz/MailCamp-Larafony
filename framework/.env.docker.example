# Docker Environment Variables for Larafony Framework
# Copy this file to .env.docker and adjust values as needed

# PHP Version
PHP_VERSION=8.5

# Database Configuration
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=larafony_test
DB_USERNAME=larafony
DB_PASSWORD=secret

# MySQL Root Password
MYSQL_ROOT_PASSWORD=root

# PCOV Configuration (code coverage driver)
# PCOV is built from source and enabled by default in the test container
# No additional configuration needed - faster than Xdebug
